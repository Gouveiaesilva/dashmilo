[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."weekly-report"]
  schedule = "@hourly"

[functions."keep-alive"]
  schedule = "*/5 * * * *"

# Redirecionar chamadas da API para a função
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Headers de segurança
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Netlify Identity - Configurações
[Identity]
  # Habilitar cadastro de novos usuários
  # Você pode desabilitar depois de criar os usuários necessários
